# Anthropic API key — never commit the real .env file
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional overrides
PORT=3001
ANTHROPIC_MODEL=claude-sonnet-4-6

# Comma-separated origins for CORS (optional; *.vercel.app is allowed by default)
# ALLOWED_ORIGINS=https://edu-nexus-theta.vercel.app,http://localhost:8080
